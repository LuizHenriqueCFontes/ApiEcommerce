{
  "name": "Spring Boot + Java 21 + MySQL",
  "image": "mcr.microsoft.com/devcontainers/base:jammy",

  "features": {
    "ghcr.io/devcontainers/features/java:1": {
      "version": "21"
    },
    "ghcr.io/devcontainers/features/maven:1": {
      "version": "latest"
    }
  },

  "postCreateCommand": "sudo apt-get update && sudo apt-get install -y mysql-server mysql-client && sudo service mysql start && sudo mysql -e \"CREATE USER IF NOT EXISTS 'user'@'%' IDENTIFIED BY 'user';\" && sudo mysql -e \"CREATE DATABASE IF NOT EXISTS mydb;\" && sudo mysql -e \"GRANT ALL PRIVILEGES ON *.* TO 'user'@'%';\" && sudo mysql -e \"FLUSH PRIVILEGES;\"",


  "customizations": {
    "vscode": {
      "extensions": [
        "vscjava.vscode-java-pack",
        "vmware.vscode-spring-boot",
        "vscjava.vscode-maven"
      ]
    }
  },

  "forwardPorts": [8080, 3306],
  "remoteUser": "vscode"
}
